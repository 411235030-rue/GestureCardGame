name: Set 50ms carousel speed

on:
  push:
    paths:
      - '.github/workflows/set-scroll-50ms.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set 50ms card speed
        run: python .github/scripts/set_scroll_50ms.py
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Test carousel at 50ms per card"
          git push
